Meteor.startup(function() {
	// if (Meteor.users.find().count() === 0) {
	// 	// TODO: default users
	// }
	// if (Meteor.users.find({ email: 'staff@mathfold.org' }).count() === 0) {
	// 	Accounts.createUser({
	// 		email: 'staff@mathifold.org',
	// 		password: 'dlofihtam8'
	// 	});
	// }
});
